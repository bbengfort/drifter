# Configuration for the Drifter app

# Runtime configuration
debug:   true
testing: false

# Mongo configuration
mongo:
    database: cps
    host: localhost
    port: 27017
    collection: products

# Phoneix Configuration
api_root: https://local.api.cobrain.com
#api_key:
